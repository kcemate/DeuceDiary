# Content archive samples

The old `content/` tree contained 48,390 generated launch-prep and queue markdown files (~190 MB). It was removed from the app repository because it is generated content, not product source.

A small representative sample is kept here for reference. If full historical content is needed, recover it from Git history before commit `67581080` or from the agent/content generation pipeline.
