name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # -------------------------------------------------------
      # Deploy step — wire up ONE of the following:
      #
      # RAILWAY:
      #   1. Go to Railway dashboard → project → Settings → Deploy Hooks
      #   2. Copy the webhook URL
      #   3. In GitHub repo → Settings → Secrets → add RAILWAY_DEPLOY_HOOK
      #   4. Uncomment the Railway step below
      #
      # RENDER:
      #   1. Go to Render dashboard → service → Settings → Deploy Hook
      #   2. Copy the webhook URL
      #   3. In GitHub repo → Settings → Secrets → add RENDER_DEPLOY_HOOK
      #   4. Uncomment the Render step below
      # -------------------------------------------------------

      - name: Deploy (placeholder)
        run: echo 'Deploy to Railway/Render — configure DEPLOY_HOOK secret'

      # - name: Deploy to Railway
      #   run: curl -X POST "${{ secrets.RAILWAY_DEPLOY_HOOK }}"

      # - name: Deploy to Render
      #   run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
