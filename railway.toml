[build]
builder = 'nixpacks'
buildCommand = 'npm ci && npm run build'

[deploy]
startCommand = 'npm run db:push -- --force && node dist/index.js'
healthcheckPath = '/api/health'
healthcheckTimeout = 120
restartPolicyType = 'on_failure'
