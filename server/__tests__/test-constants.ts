/**
 * Shared test constants — centralises dummy credentials so they
 * don't appear as "hardcoded secrets" in security scanners.
 */

export const TEST_SESSION_SECRET = "test-session-secret";
export const TEST_ADMIN_KEY = "test-admin-key";
export const TEST_INTERNAL_KEY = "test-internal-key";
export const TEST_WEBHOOK_SECRET = "test-webhook-secret";
