import { describe, it, expect } from "vitest";
import { BattleChallengeModal } from "./battle-challenge-modal";

describe("BattleChallengeModal", () => {
  it("exports BattleChallengeModal as a function", () => {
    expect(typeof BattleChallengeModal).toBe("function");
  });
});
